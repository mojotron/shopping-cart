import { render, screen } from "@testing-library/react";
import Shop from "../Shop";

describe("Shop page component", () => {
  test("render spinner while waiting request", () => {});
  test("render product items on resolved request", () => {});
  test("render error on rejected request", () => {});
});
